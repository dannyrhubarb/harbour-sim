<!--
PR title: please use Conventional Commits format, e.g. `fix(keel): clamp yaw
damping to a minimum` or `feat(ropes): add bow line fairlead`. GitHub uses
the PR title as the squash-merge commit message, so a conventional title
here keeps main's history conventional too. See CLAUDE.md's Git workflow
section for the full convention (types, scopes, breaking changes).
-->

## Summary

## Test plan
